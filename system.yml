---
  - name: Get system up and running
    hosts: all
    remote_user: vagrant
    roles:
      - basics


  - name: Install development environment
    hosts: all
    remote_user: vagrant
    roles:
      - i3wm
